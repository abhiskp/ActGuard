services:
  actguard:
    build: .
    ports:
      - "8000:8000"
    environment:
      ACTGUARD_ADMIN_TOKEN: ${ACTGUARD_ADMIN_TOKEN:-}
    volumes:
      - actguard-data:/app/data

volumes:
  actguard-data:
